<Window x:Class="WpfApp2.Parts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Parts" Height="305.114" Width="601.705">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="220"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid Name="PartsGrid" ItemsSource="{Binding}" Grid.Row="0" Height="auto" Width="auto"/>
        <StackPanel Grid.Row="1" Height="46" VerticalAlignment="Bottom">
            <Button Name="ChosePart" Click="ChosePart_Click" Height="40" Content="Выбрать" HorizontalAlignment="Left" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
